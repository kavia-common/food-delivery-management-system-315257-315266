# Core URLs (already present in real env in this environment)
BACKEND_URL=
FRONTEND_URL=
SITE_URL=

# CORS
ALLOWED_ORIGINS=http://localhost:3000
ALLOWED_HEADERS=Content-Type,Authorization
ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_MAX_AGE=3600

# Server
HOST=0.0.0.0
PORT=3001

# Database (choose ONE approach)
# Preferred: single SQLAlchemy URL (psycopg driver)
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/food_delivery

# OR fallback pieces if DATABASE_URL is not provided:
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=food_delivery
